// import { ADD_FEATURE, REMOVE_FEATURE } from "../reducer/carReducer";

// //  create store        import
// // wrap around Provider  import Provider
// // connect to dispatch  import connect

// const store = createStore(carReducer);

// export const removeFeature = (feature) => {
//   //dispatch an action here to remove a feature/feature
//   return {
//     type: REMOVE_FEATURE,
//     payload: feature,
//   };
// };

// export const buyItem = (feature) => {
//   // dispatch an action here to add an item/feature
//   return { type: ADD_FEATURE, payload: feature }; //  sent to the reducer
// };
// //                                           pass props in additionalFeatures   buyItem = {props.buyItem}
